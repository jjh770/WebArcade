# 게임 서버 WebSocket 주소. 이 파일을 .env로 복사해 값을 채운다.
# 비워두면 로컬 개발 기본값(ws://<현재 호스트>:8080)을 쓴다 — 로컬에선 설정 불필요.
#
# ⚠️ 배포 시 페이지가 https라면 반드시 wss:// 여야 한다(ws://는 브라우저가 차단).
# VITE_ 접두사가 붙은 값은 번들에 그대로 박혀 공개된다 — 비밀값 금지.
VITE_WS_URL=wss://your-arcade-server.example.com
