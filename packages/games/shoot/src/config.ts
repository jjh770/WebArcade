/* ============================================================
   에임 사격 config — 게임 튜닝값을 데이터로 분리
   ------------------------------------------------------------
   앞의 다섯 게임과 같은 패턴(config.ts). 로직은 안 건드리고 여기서 조절한다.

   게임 규칙: 표적이 하나씩 떠올랐다 수명이 다하면 사라진다. 뜬 표적을 **쏜다.**
   빨리 맞힐수록 점수가 높고, 빈 곳을 쏘면 깎인다. 제한시간이 끝나면 판이 끝난다.

   ⚠️ **에임 추적과 형제지만 재는 능력이 다르다.** 저쪽은 움직이는 표적을 놓치지 않고
      붙드는 힘(지속력)이고, 이쪽은 뜨는 순간 정확히 찍는 힘(반응 속도와 정확도)이다.
      그래서 난이도 곡선도 일부러 반대로 잡았다 — **저쪽은 표적이 작아지고 이쪽은
      표적 크기가 고정이다.** 여기서 조여드는 것은 크기가 아니라 **시간**이다
      (수명이 짧아지고 출현 간격이 좁아진다). 두 게임이 같은 곡선을 쓰면 다른 이름을
      단 같은 게임이 된다.

   ⚠️ 기록은 점수다(에임 추적·숫자 야구와 같은 쪽). 시간은 고정이다.
   ============================================================ */

import type { GaugeAlarm, ScoreDirection, ScoreUnit } from "@arcade/shared";

export const shootConfig = {
  id: "shoot",
  title: "에임 사격",
  description: "떠오르는 표적을 쏜다. 빨리 맞힐수록 높고, 헛방은 깎인다",

  /** 순위 방향: 점수가 클수록 좋음. 다섯 게임 모두와 같아 서버는 그대로다. */
  scoreDirection: "higher" as ScoreDirection,
  scoreUnit: "points" as ScoreUnit,
  /** HUD 게이지 이름. 이 게임의 게이지는 명중률이다. */
  gaugeLabel: "명중률",
  /** 경고색이 붙는 쪽. 명중률은 **떨어질 때** 문제다. */
  gaugeAlarm: "empty" as GaugeAlarm,

  /** 화면(캔버스) 논리 크기(px). app의 LOGICAL_WIDTH/HEIGHT와 일치시킬 것. */
  screenWidth: 800,
  screenHeight: 800,

  /* ---- 시간 --------------------------------------------------------------
     60초를 10초씩 여섯 구간으로 나눈다(2026-08-17 사용자 결정: "사격 60초로 하자
     폭주 구간은 마지막 10초로 하고 10초마다 조금씩 늘게 하자").

     ⚠️ 난이도의 축이 **표적 번호에서 시각으로** 바뀌었다. 예전에는 "몇 번째 표적인가"로
        간격을 정했는데, "10초마다"는 시계로 재는 말이라 그대로는 표현이 안 된다.
        ⭐ 서로 물지 않는다: i번째 표적의 간격은 **그 표적이 뜬 시각**으로 정해지고,
           그 시각은 앞 표적들의 간격만 더하면 나온다(targets.ts의 bornTickFor). */

  /** 제한시간(tick). 3600 = 60초. */
  timeLimitTicks: 60 * 60,
  /** 한 구간의 길이(tick). 600 = 10초. */
  phaseTicks: 10 * 60,

  /* ---- 표적 --------------------------------------------------------------
     ⚠️ **크기는 끝까지 고정이다.** 위 머리말 참조 — 조여드는 건 시간뿐이다. */

  /** 표적 반지름(px). 800 판에서 지름 68px. */
  radius: 34,

  /** 구간마다 표적이 뜨는 간격(tick). 앞에서부터 10초씩이다.
   *  75 ≈ 1.25초로 시작해 조금씩 좁아지다가, **마지막 구간에서 16tick(0.27초)으로
   *  뚝 떨어진다** — 그게 폭주 구간이다. */
  phaseIntervals: [75, 62, 52, 44, 36, 16] as readonly number[],

  /** 구간마다 표적이 떠 있는 시간(tick).
   *  ⚠️ **간격과의 대소가 곧 동시에 뜨는 개수다.** 수명 < 간격이면 하나씩(앞의 것이 지고
   *     다음이 뜬다), 수명 > 간격이면 겹친다. 앞의 세 구간은 일부러 수명을 간격보다
   *     짧게 둬서 **하나씩** 겨누게 하고, 네 번째부터 겹치기 시작해 마지막에 폭주한다
   *     (60/16 ≈ 넷). 처음에 이걸 거꾸로 잡았다가 판이 시작부터 둘씩 겹쳤었다. */
  phaseLives: [70, 58, 48, 52, 58, 60] as readonly number[],

  /** 판 가장자리 여백(px). 표적 **중심**이 이 안쪽에만 놓인다 — 모서리에 반쯤 걸친
   *  표적은 실제로 쏠 수 있는 면적이 줄어 그 표적만 부당하게 어려워진다.
   *  ⚠️ **여백은 시야가 튈 자리이기도 하다.** 60이던 시절엔 표적 아래 끝과 판 끝 사이가
   *     26px뿐이라, 한 발(70px) 만에 아래쪽 표적이 화면 밖으로 통째로 나갔다
   *     (2026-08-17 사용자: "한발 쏘니까 초점이 아예 나가버렸어 과녁 자체가 안 보여").
   *     지금은 `margin - radius`(71px)가 시야의 여유이고, recoilMax가 그 안에 있다.
   *     ⭐ 셋의 관계는 tests/Shoot.test.ts가 지킨다 — 하나를 올리면 거기서 걸린다. */
  margin: 105,
  /** 직전 표적에서 이만큼(px)은 떨어져 뜬다. 이게 없으면 표적이 제자리 근처에 이어
   *  떠서, 손목을 튕기는 게임이 아니라 한 점에서 미세 조정하는 게임이 된다.
   *  ⚠️ 늘 지켜지지는 않는다 — 후보를 몇 번만 뽑아 보고 안 되면 마지막 것을 쓴다
   *     (targets.ts 참조). 난수 소비 횟수를 고정하는 값이 더 크다. */
  minGap: 260,

  /* ---- 점수 --------------------------------------------------------------
       명중  = hitPoints + speedBonus × (남은 수명 비율)
       헛방  = -missPenalty
       놓침  = 0

     뜨자마자 맞히면 100, 사라지기 직전에 맞히면 50이다.

     ⚠️ **헛방 감점이 이 게임의 중심 규칙이다.** 없으면 최적 전략이 "판 전체를 최대한
        빠르게 연타"가 되어 조준이라는 게 통째로 사라진다. 놓친 표적에 따로 벌을 주지
        않는 것도 같은 이유의 뒷면이다 — 못 맞히면 점수를 못 버는 것으로 이미 충분하고,
        거기에 벌까지 얹으면 어려운 표적을 포기하는 판단이 과하게 손해가 된다.
     ⚠️ 점수는 0 아래로 안 내려간다. 순위표에 음수가 오르면 읽는 사람이 규칙을 의심한다. */

  /** 맞히면 무조건 받는 점수. */
  hitPoints: 50,
  /** 빠르기로 더 받는 최대 점수. 뜬 직후가 최대, 사라지기 직전이 0. */
  speedBonus: 50,
  /** 빈 곳을 쐈을 때 깎이는 점수. */
  missPenalty: 30,

  /* ---- 반동 --------------------------------------------------------------
     ⚠️ **이것은 연출이 아니라 규칙이다.** 쏘면 시야가 튀고, 튄 자리가 그대로 맞는
     자리다 — 보이는 곳과 맞는 곳은 언제나 같다.

     ⚠️ **움직이는 건 조준점이 아니라 세상이다.** 처음엔 조준점을 밀어 올렸는데
     (2026-08-17), 그러면 마우스와 조준점이 갈라져 "내 마우스가 곧 조준점"이라는
     가장 기본이 깨진다. 실제 총 게임의 반동은 **카메라가 튀는 것**이고, 그래서
     조준점은 화면에 붙박인 채 **판이 통째로 흔들려 내려온다.** 사용자가 정확히
     이걸 짚었다: "내 마우스는 무조건 조준점이 되어야 하고 화면이 이동하는 느낌".

     ⚠️ **무작위가 아니라 고정 패턴이다.** 두 가지 이유: ① 난수를 쓰면 판정에 난수가
     섞인다(SeededRNG 주석의 금지 사항) ② 무작위 반동은 외울 수가 없어 실력이 아니라
     운이 된다. 실제 총 게임(CS 계열)도 스프레이 궤적을 고정해 두고, 그걸 외워 끌어내리는
     것이 곧 실력이다. 여기서도 n번째 연사의 밀림이 언제나 같다. */

  /** 한 발에 시야가 튀는 거리(px). 총구가 위로 들리므로 **판은 아래로 밀려 내려온다.**
   *  ⚠️ **몇 발째든 늘 같다.** 한동안 연사할수록 커지게(45 + 15×연사수) 뒀는데, 그러면
   *     후반에 표적이 0.25초마다 뜨는 구간에서 연사가 한 번도 안 끊겨 반동이 상한에
   *     붙박였다 — 전반 45px / 후반 86px로 두 배가 됐고, **후반 폭주를 만든 표적 간격이
   *     반동까지 끌어올려** 난이도가 두 겹으로 겹쳤다. 2026-08-17 사용자가 그걸 느끼고
   *     "반동은 고정으로, 대신 좀 세게"를 골랐다. 되살리지 말 것. */
  recoilKick: 50,
  /** 쌓인 튐의 상한(px). 빠르게 이어 쏘면 앞의 것이 다 안 가라앉은 채로 더해지는데,
   *  이건 세기가 커지는 게 아니라 **남은 것이 겹치는 것**이다 — 그 겹침만 여기서 자른다.
   *  ⚠️ **`margin - radius`보다 작아야 한다.** 이걸 넘으면 판 가장자리의 표적이 화면
   *     밖으로 나가 「보이는데 못 맞히는」이 아니라 **아예 안 보이는** 표적이 생긴다. */
  recoilMax: 68,
  /** 한 tick마다 남는 밀림의 비율. 지수 감쇠라 처음엔 빨리, 끝에선 부드럽게 잦아든다
   *  (곧게 줄이면 끝에서 뚝 끊긴다).
   *  ⚠️ 이 값이 **탭과 연사를 가른다.** 0.9로 뒀더니 표적 간격만큼만 쉬어도 반동이 다
   *     풀려서 있으나 마나였다(보정 없이도 명중률 93%). 0.95에서는 한 발 쏘고 0.25초
   *     안에 다음을 쏘면 절반이 남아 있어, **탭은 정확하고 연사는 튄다**. */
  recoilRecover: 0.95,
  /** 이만큼(tick) 안 쏘면 연사가 끊긴 것으로 보고 **궤적을** 처음으로 되돌린다. 40 ≈ 0.67초.
   *  ⚠️ 세기와는 무관하다 — 되돌아가는 건 좌우로 흔들리는 순서뿐이다. */
  recoilResetTicks: 40,
  /** 좌우로 흔들리는 고정 궤적(튐 대비 비율). 위로만 튀면 아래로 곧게 끌어내리면
   *  되어서 외울 게 없다 — 좌우가 섞여야 궤적이 「패턴」이 된다.
   *  ⚠️ 첫 항이 0인 건 일부러다. 쉬었다 쏘는 첫 발은 **곧게 위로** 튀어야 예측이 선다. */
  recoilDrift: [0, -0.22, 0.3, 0.45, -0.35, -0.55, 0.6, 0.3, -0.28, 0.5] as readonly number[],

  /* ---- 시점 --------------------------------------------------------------
     ⚠️ **PC(마우스 잠금)에서만 쓰인다.** 손가락은 절대 조준이라 판이 화면에 꼭 맞아야
        하고, 물러나면 가리킨 자리와 판이 어긋난다 — 폰은 늘 배율 1이다. */

  /** 시선을 돌리는 동안 판을 이만큼 줄여 그린다. 1이면 지금까지처럼 판이 화면에 꼭 맞는다.
   *  ⚠️ **난이도를 건드리지 않는다.** 마우스가 움직인 양도 반동도 판 좌표로 재므로,
   *     물러나도 손이 판을 훑는 속도와 맞히는 어려움은 그대로다. 바뀌는 건 한눈에
   *     들어오는 넓이뿐이다.
   *  ⚠️ 조준점이 화면 한가운데에 못 박혀 있어서, 판 구석을 겨누면 판이 그만큼 밀려난다.
   *     1이던 시절엔 그때 판의 절반이 화면 밖으로 나가고 나머지 절반은 빈 곳이었다
   *     (2026-08-18 사용자: "지금 너무 가까워서 표적을 따라가기가 벅차"). 0.7이면
   *     구석을 겨눠도 판의 3/4가 남고, 한가운데를 보면 판 전체가 들어온다. */
  viewZoom: 0.7,

  /** 배경(사거리 동심원)이 표적을 따라 움직이는 비율. 1보다 작아야 **멀리 있는 것처럼**
   *  보인다 — 가까운 것이 많이, 먼 것이 적게 움직이는 게 깊이감의 전부다(시차).
   *  ⚠️ 이건 순수 연출이다. 배경은 맞힐 것이 아니므로 어긋나도 규칙을 안 건드린다. */
  backdropParallax: 0.45,
} as const;
