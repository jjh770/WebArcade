/* ============================================================
   커브 피버 config — 게임 튜닝값을 데이터로 분리
   ------------------------------------------------------------
   죽림고수와 같은 패턴(config.ts). 로직은 안 건드리고 여기서 조절한다.

   게임 규칙: 계속 앞으로 나아가는 선을 좌/우 회전만으로 조종한다.
   지나온 자리에 꼬리가 남고, 아무 꼬리(내 것이든 남의 것이든)나 벽에
   부딪히면 죽는다. 마지막 생존자가 승리.
   ============================================================ */

import type { ScoreDirection } from "@arcade/shared";

export const curveConfig = {
  id: "curve",
  title: "커브 피버",
  description: "좌우로 꺾어 선을 그린다. 꼬리에 부딪히면 죽는다",

  /** 순위 방향: 오래 살아남을수록 좋음(생존 tick). 죽림고수와 같다. */
  scoreDirection: "higher" as ScoreDirection,

  /** 화면(캔버스) 논리 크기(px). app의 LOGICAL_WIDTH/HEIGHT와 일치시킬 것. */
  screenWidth: 800,
  screenHeight: 800,

  /** 벽 여백(px). 이 안쪽이 플레이 가능 영역 — 벽에 닿으면 죽는다. */
  wallMargin: 20,

  /** 전진 속도(px/tick). 곡선이 부드럽도록 회전각과 함께 맞춘다. */
  speed: 2,

  /** 한 tick에 도는 각도(라디안). 좌/우 입력이 있을 때만 적용.
   *  작으면 완만한 대회전, 크면 급회전. 0.05 ≈ 2.9도/tick. */
  turnRate: 0.05,

  /** 선 두께(px) = 충돌 판정 반경의 2배. 굵을수록 부딪히기 쉽다. */
  lineWidth: 4,

  /** 매판 랜덤 장애물(선분 벽). 시드에서만 파생되므로 모두가 같은 판을 받는다
   *  — 플레이어와 무관한 "공통 월드"다(죽림고수의 공통 화살과 같은 성격).
   *  빈 네모에선 벽만 크게 돌면 안 죽으니, 안에 벽 조각을 흩뿌려 길을 좁힌다. */
  obstacles: {
    /** 전체 벽 조각 개수. */
    count: 9,
    /** 이 중 몇 개는 바깥 벽에 붙여 안쪽으로 뻗게 한다(벽 따라 도는 "고속도로"를 끊는다).
     *  나머지는 판 안쪽에 떠 있는 조각. count보다 크면 count로 맞춰진다. */
    wallAttachedCount: 4,
    /** 각 조각의 길이 범위(px). */
    minLength: 60,
    maxLength: 190,
    /** 떠 있는 조각의 끝점을 바깥 벽에서 최소 이만큼 띄운다(테두리와 붙어 보이지 않게). */
    edgeMargin: 60,
    /** 시작 위치를 장애물에서 이만큼 떨어진 곳에서만 뽑는다. 스폰 직후 처박힘 방지.
     *  랜덤 방향으로 출발해도 반응할 여유가 있도록 넉넉히 준다. */
    spawnClearance: 130,
    /** 안전한 스폰 자리를 못 찾을 때 포기하는 시도 횟수(무한 루프 방지). */
    maxSpawnAttempts: 80,
    /** 시작 방향을 정할 때, 몇 방향을 후보로 훑어 "가장 트인 쪽"을 고를지.
     *  랜덤 방향이면 벽·장애물을 정면으로 향해 즉사할 수 있어, 주변에서 제일
     *  먼 방향으로 출발시킨다. */
    headingSamples: 24,

    /** 꽉 찬 원 기둥 개수. 선분과 달리 원 안 전체가 죽음 지대다(닿으면 죽음). */
    circleCount: 3,
    /** 원 기둥 반지름 범위(px). */
    circleMinRadius: 22,
    circleMaxRadius: 46,

    /** 다각형 테두리(속 빈 도형) 개수. 원과 달리 **윤곽선만** 벽이라 안으로
     *  들어갔다 나올 수 있다("도형 안에 갇히지 마"). 변을 선분으로 쪼개 판정한다. */
    polygonCount: 2,
    /** 다각형 반지름(중심~꼭짓점) 범위(px). */
    polygonMinRadius: 40,
    polygonMaxRadius: 72,
    /** 다각형 변 개수 범위(3=삼각형 ~ 5=오각형). */
    polygonMinSides: 3,
    polygonMaxSides: 5,
  },

  /** 장애물 선 두께(px). 꼬리보다 굵게 두어 "벽"으로 읽히게. */
  obstacleWidth: 6,

  /** 충돌 면제 구간(최근 N개 꼬리점). 이게 없으면 자기 머리 바로 뒤 꼬리에
   *  매 tick 부딪혀 즉사한다 — 머리와 가장 가까운 꼬리는 원래 붙어 있으니까. */
  selfImmuneTrailPoints: 12,

  /** 시작 시 벽에서 띄우는 최소 거리(px). 스폰 직후 벽에 처박히지 않게. */
  spawnInset: 120,

  /** 「스릴 게이지」 — 벽·꼬리·장애물에 아슬아슬하게 스치면(니어 미스, 충돌 직전) 게이지가 찬다.
   *  꽉 차면(=1) 조준한 상대 1명에게 방해 디버프를 쏘고 0으로 리셋된다(아래 fire 참조).
   *  안전빵 플레이는 게이지가 안 차게 해 공격적 스치기를 유도한다. */
  nearMiss: {
    /** 충돌 경계에서 이 거리(px) 안으로 들어오면 스침으로 친다. 경계 안(음수)은 죽음이라 제외. */
    grazeBand: 14,
    /** 스치는 tick마다 게이지 증가량(꽉 참=1.0). 경계에 붙을수록 이 값에 가깝고,
     *  밴드 바깥 끝이면 0에 가깝다(가까울수록 빨리 찬다). 0.05 = 최대 근접 시 20tick(≈0.33초).
     *  ⚠️ 0.02였을 땐 한 목숨 안에 게이지가 최대 ~0.47까지만 차 발사가 거의 안 터졌다.
     *     0.05로 올려 "적극적으로 위빙하며 스치는" 플레이가 한 판에 발사에 도달하게 했다. */
    gaugeGain: 0.05,
    /** 벽에 평행하게 직진으로 붙여 무한 충전하는 익스플로잇 방지. 마지막으로 방향을
     *  튼 뒤 이 tick(180=3초)이 지나면, 스치고 있어도 게이지가 더는 안 찬다.
     *  → 능동적으로 꺾으며 스치는 플레이만 보상한다. */
    turnGraceTicks: 180,
  },

  /** 게이지가 꽉 차면 조준한 상대 1명에게 거는 방해 디버프 풀. 발사 순간 앱이 이 중
   *  하나를 랜덤으로 뽑아 우측 관전 중인 상대에게 보낸다(서버는 내용 모르고 그 1명에게 중계).
   *  결정론 안전: 모든 디버프는 맞은 쪽 자기 입력/화면만 바꾼다(각 클라 시뮬은 원래 독립).
   *  - 조작계(게임이 처리): invert(좌우 반전), sluggish(회전 둔화)
   *  - 시각계(앱이 victim 화면에 처리): blur(시야 흐림), shake(화면 흔들림), cloud(구름 가림)
   *  kind 슬러그는 서버 검증 정규식 [a-z0-9_-]{1,32}과 맞물린다. durationMs는 상한 10000ms. */
  fire: {
    debuffs: [
      { kind: "blur", durationMs: 2500 },
      { kind: "sluggish", durationMs: 3000 },
      { kind: "shake", durationMs: 2000 },
      { kind: "cloud", durationMs: 3000 },
      { kind: "invert", durationMs: 2500 },
    ],
    /** sluggish 동안 회전 속도 배수(1=정상). 낮을수록 굼떠 급회전을 못 한다. */
    sluggishTurnMult: 0.4,
  },
} as const;
