# DESIGN.md — 결정론적 웹 멀티 아케이드 프레임워크

> 이 문서는 프로젝트의 아키텍처, 게임 플로우, 그리고 반드시 지켜야 할 설계 불변식을 정의한다.
> 코드를 작성하기 전에 이 문서를 읽고, 여기 적힌 원칙과 충돌하는 구현은 하지 않는다.

---

## 0. 한 문장 요약

같은 시드로 모두가 같은 세계(화살 패턴 등)를 각자 화면에서 겪고, 게임 판정은 결과(생존/사망)만 동기화하는
**결정론적 동기화 기반 웹 멀티 아케이드 프레임워크**. 관전 품질을 위해 플레이어 위치는 10Hz 스냅샷으로 별도 중계한다.
그 위에 게임을 여러 개 얹는다. 첫 게임은 "죽림고수"(화살 피하기).

핵심 설계 철학: **설계 판단은 사람이, 반복 검증은 AI가.**

---

## 1. 기술 스택

- **언어**: TypeScript (클라이언트·서버·공유 계약 전부)
- **렌더링**: Canvas 2D로 시작. `IRenderer` 인터페이스로 추상화하여, 필요 시 PixiJS(WebGL)로 교체 가능하게 둔다.
- **클라이언트 게임 루프**: 고정 타임스텝 + **rAF + `setInterval` 폴백** (아래 참조)
- **네트워크**: WebSocket (브라우저 표준). Photon 아님.
- **서버**: Cloudflare Workers + Durable Objects. **방 하나 = 오브젝트 하나**인 작은 authoritative 서버.
- **빌드 타깃**: 웹 (링크 클릭 → 바로 플레이). 정적 클라 + WebSocket 서버 분리 배포.
- **구조**: 모노레포 (shared / core / games / edge / app)
- **테스트**: Vitest (`npm test`) — 순수 로직은 Node에서, 서버 전송 계층은 **workerd 안에서**
  (DO·하이버네이션·알람은 모킹하면 검증하려던 것이 사라진다)

### 왜 rAF만으로는 안 되는가 (GameLoop)

`requestAnimationFrame`은 **탭이 백그라운드로 가면 브라우저가 멈춘다.** 싱글이면 상관없지만
멀티에서는 치명적이다 — 다른 탭을 보다 돌아오면 내 월드만 몇 초 뒤처져 있고, 남들은 이미
저만치 진행해 있다. 결정론이 깨진 게 아니라 **내 tick이 안 흐른 것**이지만 결과는 똑같이 망가진다.

그래서 `GameLoop`는 두 축으로 돈다:
- **rAF**: 화면이 보일 때의 부드러운 렌더 (자유 프레임레이트, 보간 alpha 사용)
- **`setInterval` 폴백**: 백그라운드에서도 tick을 계속 전진시킨다

둘 다 "예약된 시작 시각(epoch)부터 흐른 실시간"에서 tick을 계산하므로 **중복 전진하지 않는다.**
tick의 유일한 근거는 벽시계 경과 시간이지, 콜백이 몇 번 불렸는지가 아니다.

> 왜 Unity가 아닌가: 원래 만들고 싶었던 것이 TETR.IO 같은 "웹이 곧 게임"인 웹 네이티브 경험이기 때문.
> C#/Unity 아키텍처 감각(IRepository, FSM, 오브젝트 풀링)은 TypeScript로 1:1 이식된다.

---

## 2. 결정론 불변식 (THE INVARIANT — 절대 규칙)

이 프로젝트의 심장. 이 규칙이 깨지면 클라이언트마다 화살이 달라져 게임이 무너진다.

> **게임 결과에 영향을 주는 모든 것은 시드와 tick에서만 파생된다.**
> **시스템 시계·전역 상태·실측 delta는 결과 계산에 절대 끼어들지 않는다.**

구체 규칙:

1. **전역 난수 금지.** `Math.random()`을 게임 로직에 쓰지 않는다. 시드로 초기화한 `SeededRNG`(mulberry32)
   인스턴스를 스포너 등이 소유한다. 시각 효과(파티클·화면 흔들림 등 게임 결과에 무관한 것)에만 `Math.random()` 허용.
2. **고정 타임스텝.** 게임 로직 업데이트는 항상 고정 간격(1/60초)으로만 전진한다. `update(tick)`은 실측
   `deltaTime`을 받지 않는다. 렌더는 자유 프레임레이트로 하되 보간(alpha)만 사용.
3. **난이도·스폰 등 모든 진행은 tick의 함수.** `Date.now()`로 난이도를 올리지 않는다. 예: `difficulty = floor(tick / 300)`.
4. **판정은 로컬, 동기화는 결과만.** 피격 판정은 각 클라이언트가 자기 화면에서 내린다. 부동소수점 완전 결정론까지
   갈 필요 없음 — 화살 패턴만 시각적으로 동일하면 충분. 네트워크로는 "죽음/생존시간"만 전송.
5. **실시간 epoch 기반 따라잡기.** tick은 예약 시작 시각 이후 흐른 실시간에서 계산한다. 백그라운드에서
   놓친 tick은 한 번에 최대 300개씩 여러 호출에 나눠 따라잡아 AFK로 월드가 뒤처지거나 UI가 오래 멈추지 않게 한다.

> "어디까지 결정론으로 맞추고 어디부터 로컬에 맡길지 선을 긋는" 것이 핵심 설계 판단 지점이다.

---

## 3. 아키텍처 — 계층과 의존성 방향

### 의존성 규칙 (OPEN-CLOSED)

> **모든 의존성 화살표는 위로만 향한다. 코어는 게임을 모른다.**

- `shared` (계약: IGame, 메시지 타입) ← 모두가 의존
- `core` (엔진: 루프·RNG·네트워크·FSM) ← games, server가 의존. **games를 import하지 않는다.**
- `games/*` (게임별 규칙) → core, shared에 의존
- `server` → shared에 의존 (게임 내용을 모름. gameId 문자열만 중계)
- `app` (진입점) → 게임들을 GameRegistry에 등록

새 게임 추가 시 core는 한 줄도 바뀌지 않아야 한다. 이 불변이 깨지면 추상화가 새는 것이다.

### 폴더 구조

```
WebArcade/
├─ packages/
│  ├─ shared/src/          # 계약 — 구현 없음. 모두가 의존한다.
│  │  ├─ IGame.ts          # 게임이 구현할 인터페이스
│  │  ├─ IRenderer.ts      # 렌더 계약 (⚠️ core가 아니라 여기. 아래 근거 참조)
│  │  ├─ SeededRNG.ts      # mulberry32 시드 PRNG (⚠️ core가 아니라 여기 — 아래 근거)
│  │  ├─ protocol.ts       # WebSocket 메시지 타입
│  │  └─ types.ts          # InputState, PeerState, RoomState 등
│  ├─ core/src/            # 엔진 — 게임을 모름
│  │  ├─ GameLoop.ts       # 고정 타임스텝 (rAF + setInterval 폴백)
│  │  ├─ StateMachine.ts   # 게임을 모르는 범용 FSM (허용 안 된 전이는 예외)
│  │  ├─ ClockSync.ts      # RTT 최소 표본으로 서버 시각 보정
│  │  ├─ GameRunner.ts     # IGame을 받아 구동 + 멀티 뷰 렌더
│  │  ├─ net/NetClient.ts
│  │  ├─ render/Canvas2DRenderer.ts   # IRenderer 구현 (논리좌표 ↔ DPR 픽셀)
│  │  └─ input/
│  │     ├─ InputSource.ts     # 입력 소스 계약 + CompositeInput(키보드 OR 터치)
│  │     ├─ InputManager.ts    # 키보드 방향키 → InputState
│  │     ├─ TouchInput.ts      # 요소 위 포인터 → InputState (매핑은 앱이 준다)
│  │     └─ ButtonInput.ts     # 버튼 하나가 방향 하나 → InputState (좌표·매핑 없음)
│  ├─ games/jungnim/src/   # 죽림고수 (첫 게임)
│  │  ├─ JungnimGame.ts    # implements IGame
│  │  ├─ ArrowSpawner.ts   # 공통(시드) 화살 — 모두가 동일
│  │  ├─ PersonalSpawner.ts# 개인(조준) 화살 — 별도 RNG 스트림
│  │  ├─ ArrowPool.ts      # 오브젝트 풀
│  │  └─ config.ts         # 튜닝값을 데이터로 분리
│  ├─ games/curve/src/     # 커브 피버 (둘째 게임) — 좌우로 꺾어 선 긋기
│  │  ├─ CurveGame.ts      # implements IGame
│  │  ├─ obstacles.ts      # 장애물 도형(선분·원·다각형)과 충돌 판정
│  │  └─ config.ts         # 튜닝값(속도·회전각·장애물 밀도)
│  ├─ games/floor/src/     # 무너지는 바닥 (셋째 게임) — 격자 위 칸 단위 이동
│  │  ├─ FloorGame.ts      # implements IGame (「누른 순간」을 게임이 직접 만든다)
│  │  └─ config.ts         # 튜닝값(격자·붕괴 물결·자동 반복)
│  ├─ games/baseball/src/  # 숫자 야구 (넷째 게임) — 방향이 아니라 숫자를 친다
│  │  ├─ BaseballGame.ts   # implements IGame (+ typeKey — 화면과 입력만)
│  │  ├─ rules.ts          # 답 생성·판정·기회·점수 (전부 순수 함수)
│  │  └─ config.ts         # 튜닝값(자리 수·제한시간·기회·점수 곡선)
│  ├─ edge/                # 게임 서버. 게임 내용을 모름. gameId는 문자열일 뿐.
│  │  ├─ src/index.ts      # Worker 진입점 — /health, POST /rooms, /solo/*, /ws 라우팅
│  │  ├─ src/RoomObject.ts # 방 하나 = DO 하나. WS 수신·중계·alarm 회수
│  │  ├─ src/Room.ts       # 방 상태(순수) + storage 왕복(snapshot/restore)
│  │  ├─ src/BoardObject.ts# 혼자 기록 랭킹 = DO 하나(전 세계 공용). 티켓 서명·보관
│  │  ├─ src/soloRules.ts  # 혼자 기록 판정(순수) — 티켓 인코딩·시간 검사·줄 세우기·지우기
│  │  ├─ src/adminAuth.ts  # 운영자 열쇠 확인(순수). 기록 지우기 경로에만 쓴다
│  │  ├─ src/roomCode.ts   # 방 코드 발급 (문자셋이 클라 검증과 맞물림)
│  │  ├─ src/validation.ts # 모든 수신 메시지 형식·범위 검증
│  │  ├─ src/RankingService.ts
│  │  ├─ test/             # workerd 안에서 도는 통합 테스트
│  │  └─ wrangler.toml     # DO 바인딩·마이그레이션
│  └─ app/src/             # 진입점 — 위의 조각들을 엮는 곳
│     ├─ main.ts           # 배선판 — 상태·세션·네트워크·버튼을 잇고 한 판의 흐름을 부른다
│     │                     #   방과 라운드의 사실(내 신원·방장·고른 게임·결과)을 소유한다
│     ├─ serverRoutes.ts   # 서버 메시지 하나 → 앱의 행동(기대는 것을 deps로 선언)
│     ├─ navigation.ts     # 헤더 내비와 주소(해시). 방에 있는 동안은 못 새게 막는다
│     ├─ hud.ts            # 기록·게이지 표시(단위와 경고 방향은 게임이 정한다)
│     ├─ peerReport.ts     # 0.1초마다 내 위치·기록을 남에게 알린다(끊겼을 때의 최종값)
│     ├─ AppFlow.ts        # 앱 상태 전이표 (StateMachine에 주입)
│     ├─ dom.ts            # byId — 없는 id면 즉시 던진다(유일한 요소 접근 통로)
│     ├─ AppView.ts        # 무엇을 보여줄까 — 화면 전환·카드 내용
│     ├─ screenFx.ts       # 어떻게 움직일까 — 낙하·관전 전환·디버프·탄환
│     ├─ playLayout.ts     # 캔버스 표시 크기 배분(여백·조작 영역은 CSS가 정본)
│     ├─ GameSession.ts    # 한 라운드의 게임 인스턴스와 러너
│     ├─ soloPlay.ts       # 혼자 플레이 한 판(티켓·카운트다운·결과·기록 제출)
│     ├─ rankingScreen.ts  # 순위 화면 내용 채우기(전이는 main 몫)
│     ├─ peerViews.ts      # 남들의 상태 + 누구를 어느 화면에 띄우는가
│     ├─ touchControls.ts  # 판 밖 조작면 일체(입력 소스 + 세로 예산)
│     ├─ GameRegistry.ts   # ⭐ 새 게임 등록. 앞의 셋은 여기 한 줄로 끝났다
│     │                     #   (넷째는 새 입력 양식이라 계약·조작면까지 넓혔다 — 9절)
│     ├─ touchSchemes.ts   # 터치 조작 방식 + 조작면→body 클래스 표(CSS와의 유일한 접점)
│     ├─ touchHint.ts      # 카운트다운 동안 뜨는 조작 안내
│     ├─ joystick.ts       # 조이스틱 위젯의 노브 표현(입력은 TouchInput 몫)
│                          #   방향키 버튼은 표현이 CSS뿐이라 대응 파일이 없다
│     ├─ countdown.ts      # 시작 전 3초(멀티=서버시각 / 솔로=로컬시각, 재는 법만 다름)
│     ├─ serverUrl.ts      # 서버 주소 한 곳(방 접속과 순위표가 같은 값을 본다)
│     ├─ roomConnect.ts    # 방 만들기·접속 절차(화면은 안 건드림)
│     ├─ soloRanking.ts    # 혼자 기록 티켓·제출·순위표 조회(실패는 전부 null)
│     ├─ audio.ts          # 효과음을 코드로 합성(음원 파일 없음) + 켬/끔
│     ├─ soundShell.ts     # 소리 토글 버튼 + 클릭음 위임(앱 상태와 무관)
│     └─ siteContent.ts    # 공지 등 사이트 콘텐츠 데이터
├─ scripts/board.mjs       # 순위표 운영 도구(보기·지우기). 관리자 화면 대신 (10절)
└─ tests/                  # Vitest — 결정론·클럭·FSM·방·서버 통합
```

**왜 `IRenderer`가 `core`가 아니라 `shared`에 있는가?**
`games/*`가 렌더를 하려면 렌더 계약을 알아야 한다. 그 계약이 `core`에 있으면 게임이 엔진 구현체에
묶인다. 계약(`IRenderer`)은 `shared`에, 구현(`Canvas2DRenderer`)은 `core`에 두면 게임은 **"무엇을
그릴지"만 알고 "어떻게 그리는지"는 모른다.** PixiJS로 갈아끼워도 게임 코드는 한 줄도 안 바뀐다.
`IGame`이 게임 → 엔진 방향의 계약이라면, `IRenderer`는 엔진 → 게임 방향의 계약이다. 둘 다 `shared`.

> **넓힌 적이 한 번 있다 — 글자 정렬(2026-08-06).** 원시 함수 다섯 개(`clear`·`circle`·`rect`·
> `line`·`text`)로 시작했는데, 글자 폭을 잴 방법이 없으니 게임들이 중앙 정렬을 `cx - 90` 같은
> 상수로 눈대중하고 있었다(네 게임에 걸쳐 여덟 곳). 그 상수는 **글자 수가 변하면 틀린다** —
> "사망 — 생존 9.9s"와 "사망 — 생존 123.4s"가 서로 다른 만큼 어긋나 있었다. 폭을 아는 건
> 렌더러뿐이므로, 게임이 계산을 넘기지 않고 **의도**(`align`)를 넘기게 했다.
>
> ⚠️ 폭을 그대로 돌려주는 `measureText`는 **일부러 안 열었다.** 지금 있는 요구는 전부 정렬
> 하나로 풀린다. `IGame`의 선택 메서드와 같은 규칙이다 — 실제로 요구하는 게임이 나오기 전에
> 넓히지 않는다.

**왜 `SeededRNG`도 `shared`에 있는가?** 같은 이유다. 한때 `core`에 있었고, 세 게임이 **오직 이것
하나 때문에** 엔진을 import했다 — 렌더러·웹소켓·터치 입력이 든 패키지에 게임이 묶여 있었다는 뜻이다.
시드 PRNG는 엔진 기능이 아니라 결정론 계약의 일부이므로 `shared`로 옮겼다. 그 결과:

```
shared (잎)  ←  core  ←  app
      ↖ games ↗            ↖ edge
```

게임의 의존은 이제 `shared` **하나뿐**이다. "core는 게임을 모른다"의 짝인 **"게임도 엔진을 모른다"**가
성립한다. 순환도 역방향도 없다.

### IGame 계약 (잠정 — 두 번째 게임이 검증할 때까지 확정하지 않음)

```typescript
export interface IGame {
  init(seed: number): void;                       // 시드로 결정론 초기화
  update(tick: number, input: InputState): void;  // 고정 스텝 전진
  render(r: IRenderer, alpha: number): void;       // 자기 렌더
  renderSpectator(r: IRenderer, target: SpectateTarget): void;
  isPlayerDead(): boolean;                         // 로컬 사망 판정
  getPosition(): SpectateSignal;                   // 관전 중계에 실을 숫자 둘(대개 위치)
  getScore(): number;                              // 순위 기준

  // ---- 선택 메서드 (구현 안 하면 그 기능이 그냥 없는 게임이 된다) ----
  syncPeers?(peers: readonly PeerState[]): void;        // 남의 신호 반영(쌓을 게 있는 게임만)
  getGauge?(): number;                                  // 0~1. DOM HUD 게이지 줄
  consumePendingFire?(): readonly Debuff[] | null;      // 이번 스텝의 방해 발사(디버프 풀)
  applyEffect?(kind: string, durationMs: number): void; // 남의 발사에 맞았을 때
  consumePeerEvent?(): string | null;                   // 남 화면에도 보여야 할 연출(슬러그)
  applyPeerEvent?(id: string, kind: string): void;      // 남이 낸 연출이 도착했을 때
  consumeSounds?(): readonly string[] | null;           // 이번 스텝에 난 소리(슬러그)
}
```

재시작은 `init(newSeed)` 재호출로 통일한다. 관전 위치와 개인 조준 화살은 시각적 근사이며 판정에는 사용하지 않는다.

**선택 메서드는 옵셔널로 넓혔다.** 앞의 셋은 커브 피버의 방해 발사, 그다음 둘은 죽림고수의 정화
파동이 요구해서 생겼다(아래 5절 참조). **두 게임이 서로 다른 부분집합만 구현한다** — 필수로
만들었다면 양쪽 다 빈 구현을 안고 있었을 것이다.

`consumeSounds`는 소리를 붙이며 마지막으로 늘었다. **슬러그를 돌려주고 소비하는** 모양은
`consumePendingFire`·`consumePeerEvent`와 같고, core도 서버도 슬러그의 뜻을 모른다 — 소리로
바꾸는 건 앱이다. 판정이 아니라 출력이라, 여기서 무엇을 내든 tick·시드·결과는 갈리지 않는다
(회귀 테스트: 소리를 매 tick 가져가든 한 번도 안 가져가든 경로가 같다).

**잠정적으로 열어둘 지점** (두 번째 게임에서 실제로 부딪히면 넓힌다. 상상으로 미리 넓히지 않는다):
- ~~`getScore()`의 우열 방향~~ → **해소됨.** `ScoreDirection`('higher'|'lower') 타입으로 분리하여
  게임 레지스트리 항목(`scoreDirection`)에 담는다. 죽림고수='higher'.
- 입력이 4방향으로 충분한지 — **커브 피버(둘째 게임)는 좌/우만 써서 4방향의 부분집합이었다.
  즉 넓힐 필요가 없었다.** 회전 각도·연속 입력이 필요한 게임이 오면 그때 다시 본다.
- 종료 조건이 "죽음" 하나인지 (목표 도달로 끝나는 게임 대비) — 커브도 "죽음"이라 아직 유효.
- ~~관전 데이터가 숫자 둘(`PeerState {a,b}`)로 충분한지~~ → **해소됨. 안 넓혔다.**
  커브는 꼬리 이력이 필요했지만, 10Hz로 오는 위치를 게임이 로컬에 쌓아 폴리라인으로 이으니
  충분했다(`CurveGame.peerTrails`). 이력은 **받는 쪽이 만들면 되는 것**이라 wire 타입을
  키울 이유가 없었다 — 넓혔다면 대역폭만 늘었을 것이다.

### GameRunner — 코어가 게임을 모른 채 구동하는 방식

`GameRunner`는 `JungnimGame`이라는 단어를 절대 쓰지 않는다. 오직 `IGame` 메서드만 호출한다.
이것이 IRepository로 데이터 계층을 추상화한 것과 동일한 의존성 역전이다.

---

## 4. 게임 전체 플로우

접속 → 순위표 → 재시작까지의 라이프사이클. (사용자가 직접 설계한 플로우)

1. **접속 · 닉네임**: 사이트 진입 시 닉네임을 한 번 정한다. 안 정하면 랜덤 배정. 중복 허용(순위표 표시용).
   URL에 방 코드(`#WXYZ`)가 있으면 자동 입장 시도.
2. **메인 · 게임 목록**: 게임 시작 / 옵션 / 크레딧. "게임 시작" → 게임 목록(GameRegistry에서 자동 생성).
3. **로비**: 게임 선택 후 진입. 실시간 대전 찾기 / 방 만들기 / 방 코드 입력.
   - **실시간 대전 찾기는 나중에 구현** (매치메이킹은 별도 서브시스템. 코드 기반 방을 먼저 완성).
4. **방 대기실 (Ready)**: 방 만들기 → 코드 발급(I·O 제외 4자리). 코드로 참가. v1 최대 32명.
   - 준비 버튼 없음. **호스트가 시작 버튼을 누르면 즉시 시작** (TETR.IO 방식).
   - 호스트가 나가면 **남은 사람 중 가장 먼저 들어온 사람**에게 이양(서버가 입장 순서 리스트로 관리).
5. **시드 배포 · 카운트다운**: 연결 시 5회 ping/pong 중 RTT가 가장 낮은 표본으로 서버 시각을 보정한다.
   호스트 시작 → 서버가 `game_start(seed, startTime)` 브로드캐스트. 각 클라는 보정된 시각과 예약 epoch로
   카운트다운하고, 늦은 콜백도 예약 시각부터 경과한 tick을 따라잡는다.
6. **플레이**: 본인 화면 크게 + 우측에 무작위 생존자 최대 3명.
   - 화살은 각 클라가 시드로 로컬 생성. 피격 판정도 로컬.
   - 관전 대상이 죽으면 다른 무작위 생존자로 교체.
   - **레이아웃 축소 규칙(공통)**: 항상 "생존자 중 최대 3명"을 보여주되 부족하면 있는 만큼만. 1+3 → 1+2 → 1+1.
     이 규칙은 플레이 중이든 관전 중이든 동일하게 적용된다.
7. **본인 사망 시 선택지**:
   - (a) 결과창 유지 — 메인 자리에 내 결과, 우측에 관전 대상.
   - (b) 관전 — 메인 자리도 남의 화면. 관전 1명 + 관전 최대 3명(1+3→1+1 축소 규칙 적용).
8. **게임 종료**: 전원 사망 시. **상태와 무관하게 그 자리에서 순위표 표시**
   (관전 중이면 관전 화면 위에, 생존자면 플레이 화면 위에. "본인 화면 복귀" 없음).
   - 순위표 = **등수 + 닉네임 + 생존시간** (생존시간 긴 순).
9. **순위표 다음**: 같은 방 다시 하기(새 시드로 재시작 → 대기실) / 로비로 가기(방 나가기).

> **관전 대상 선택과 레이아웃은 로컬 판단이다.** 서버는 게임 화면을 시뮬레이션하지 않고, 각 클라이언트가
> 보낸 최신 위치를 방 단위 10Hz `peer_snapshot`으로 묶어 중계한다. 공통 화살은 seed/tick으로 정확히
> 재현하고, 원격 아바타와 개인 조준 화살은 위치 스냅샷을 보간한 시각적 근사다.

---

## 5. 네트워크 프로토콜 (shared/protocol.ts)

```
클라 → 서버:  join_room(code, nickname) · create_room(gameId, nickname) · start_game
             time_sync_request · player_state(a, b, ev?, sc?) · player_died(score)
             fire_effect(kind, durationMs, targetId) · return_to_ready · leave_room
서버 → 클라:  welcome(id) · time_sync_response · room_state(state, players, hostId)
             game_start(seed, startTime, gameId) · peer_snapshot(peers) · peer_died(id)
             effect_hit(from, kind, durationMs) · ranking_update(alive, ranks)
             game_over(finalRanks) · host_changed(newHostId)
```

- 서버는 게임 내용을 모른다. `gameId`는 문자열로 중계할 뿐.
  단 하나의 예외는 "이 게임의 기록이 흐른 시간인가"다(`timedGames.ts`) — 자기신고 상한 검사가
  성립하려면 그것만은 알아야 한다. 모르는 게임은 검사를 건너뛰므로 이 표가 게임 추가를 막지 않는다.
  자세한 사정은 10절.
- 화살은 네트워크를 한 번도 타지 않는다. 관전 위치만 플레이어당 10Hz로 보내며 서버가 방별 스냅샷으로 묶는다.
- `fire_effect`도 마찬가지로 **서버는 `kind`의 의미를 모른다.** 형식(`/^[a-z0-9_-]{1,32}$/`, 지속시간
  ≤10초, 대상이 접속 중인 남)만 검증하고 그 한 명에게 `effect_hit`으로 넘긴다. 저장하지 않는다.
  → **디버프 종류를 늘려도 서버는 재배포할 필요가 없다.**
- `player_state`의 `ev`도 같은 성격이다. **선택 필드**라 안 실으면 프로토콜은 예전 그대로고,
  실리면 서버가 **다음 스냅샷 딱 한 번**에 얹어 보낸 뒤 지운다(`Room.getPeerSnapshot`의 의도된
  부수효과). 위치가 더 자주 갱신돼도 아직 안 보낸 이벤트는 덮어쓰지 않는다.
  → **연출을 늘려도 서버는 재배포할 필요가 없다.**
- v1은 최대 32명으로 검증한다. 그 이상 확장은 측정 후 전송 빈도·압축·관전 정책을 다시 결정한다.

### 플레이어 간 변수 — 두 갈래

두 게임이 "잘 피한 사람에게 보상"이라는 같은 문제를 서로 다르게 푼다. 어느 쪽이든 지키는
한 가지 규칙: **공통 월드(시드에서 나오는 화살·장애물)는 아무도 건드리지 않는다.**

#### (a) 커브 피버 — 스릴 게이지 = 방해 발사 (event-injection 핸디캡)

벽·꼬리·장애물 스침 정도에 비례해 게이지가 차고, 100%면 발사. **"맞은 사람의 입력·시야만
바꾼다"** 는 규칙 하나로 결정론을 지킨다. `invert`는 좌/우 반전(회전 게임이라 축이 그것뿐),
`sluggish`는 회전 속도 ×0.4.

- **디버프 5종**: `blur`·`shake`·`cloud`(시각계 — **앱이 DOM/CSS로** victim 화면에 건다) +
  `invert`·`sluggish`(조작계 — **게임이** 자기 입력 사본에 건다). 게임은 모르는 kind를 무시하므로
  종류가 늘어도 옛 클라가 죽지 않는다.
- **랜덤은 앱에서만**: 게임은 디버프 *풀*을 돌려줄 뿐(`consumePendingFire`), 누구를 맞힐지·어떤
  디버프일지는 앱이 `Math.random`으로 고른다. 게임 `update()`에는 여전히 난수가 없다(2절 불변식).
- **조준 = 지금 내 우측 관전창에 떠 있는 사람 중 1명**. "보이는 사람만 맞힌다"가 규칙이라 화면과
  인과가 맞고, 전원 브로드캐스트보다 훨씬 덜 아프다. 정말 혼자 남으면 발사는 그냥 버려진다.
- **왜 락스텝이 필요 없나**: 각 클라의 시뮬은 원래 독립이고 판정도 로컬이다. 효과가 바꾸는 건
  그 클라의 입력뿐이라 공통 월드(시드 기반 화살·장애물)는 전 클라에서 그대로 일치한다.

#### (b) 죽림고수 — 아이템 = 자기 강화

죽림고수도 처음엔 같은 방해 발사(스침 10회)를 썼지만, **화살을 피하기도 바쁜 게임에서 "남을
때리는 것"이 화면에 잘 읽히지 않았다.** 그래서 같은 자리를 아이템으로 바꿨다 — 20~28초에 한 번
경기장 어딘가에 뜨고 10초 뒤 사라진다.

| 종류 | 확률 | 효과 | 공통 월드 |
|---|---|---|---|
| 질주 | 25% | 5초간 이동 속도 ×1.6 | 안 건드림(내 입력) |
| 쉴드 | 25% | 피격 한 번을 버틴다 | 안 건드림(내 판정) |
| 조준 정지 | 25% | 6초간 **개인 화살** 스폰 정지 | 안 건드림(원래 로컬 레이어) |
| 정화 | 25% | 반경 280px 안의 화살 삭제 | **로컬에서 지운다 ← 유일한 예외** |

- **뜨는 것은 공통, 줍는 것은 로컬**: 시각·자리·종류는 시드+tick에서만 나오고(`ItemSpawner`),
  획득 판정만 로컬이다. 그래서 **같은 아이템을 여러 명이 각자 줍는다** — 남이 가져가도 내 것은
  남는다. 선점 경쟁으로 만들면 "누가 먼저 닿았나"를 서버가 판정해야 하고, 그 순간 서버가 게임
  좌표를 알게 된다. 그 대가가 이 게임이 주는 재미보다 크다.
- **스폰 일정은 획득과 무관하다**: 주웠다고 다음 스폰이 당겨지면 사람마다 일정이 갈린다.
  `ItemSpawner.consume()`은 화면에서만 치우고 `nextSpawnTick`은 건드리지 않는다.
- **난수 스트림 분리**: 아이템은 `ITEM_SEED_SALT`로 파생한 자기 RNG를 쓴다. 공통 화살과 스트림을
  나눠 쓰면 아이템을 하나 굴릴 때마다 **화살 패턴 전체가 밀린다.**
- **정화만 예외인 이유와 처리**: 로컬에서 공통 화살을 지우므로 나를 관전 중인 사람 화면엔 그
  화살이 남는다. 지우는 쪽이 `"purge"` 슬러그를 위치에 얹어 보내고, 받은 쪽은 그 사람의
  관전창에서만 **파동 링 안쪽을 안 그린다**. 받은 쪽이 진짜로 지우면 안 된다 — 공통 풀은 자기
  게임 화면과 관전창이 함께 쓰므로 **자기 피격 판정까지 바뀐다.**

---

## 6. 서버 — 방 관리 (결정론 덕에 단순함)

서버가 들고 있는 것: 방 코드, 접속자(입장 순서 리스트), 상태, 시드, 시작 시각, gameId, 최신 관전 위치.

- `Room`: 접속자를 **순서 있는 리스트**로 관리(호스트 이양의 근거). 최대 32명, 진행 중이면 입장 거절.
- 진행 중 연결 종료는 서버 경과 tick의 사망으로 기록하고 해당 라운드 최종 순위에는 남긴다.
- `roomCode`: 코드 생성 시 I·O 제외한 문자셋 사용(1·0과 헷갈린다). `start_game`에서 시드 +
  `startTime = now + 3000` 배포 — **이 두 값이 모두에게 같아야** 같은 세계가 된다.
- `RankingService`: 생존 신호 수집, 순위 계산, `ranking_update` 중계.
- 빈 방은 60초 유예 뒤 DO의 `alarm()`이 회수한다. 새로고침 한 번에 방이 증발하면 안 되기 때문이다.

**나중에 정할 것 (지금 안 정해도 됨)**:
- 진행 중 입장을 관전으로 허용할지 / 인원 많을 때 UI 표시 기준 / 방 생성 rate limit.

---

## 7. 개발 순서 (반드시 이 순서)

추상화는 상상으로 하지 않는다. 만들고 나서 두 번째 사례로 검증하며 추출한다.

1. ✅ **코어 + shared(IGame) + games/jungnim을 처음부터 분리된 구조로 짠다.**
   죽림고수도 처음부터 `implements IGame`. 단 인터페이스는 잠정.
2. ✅ **죽림고수 싱글 완성** — GameLoop·SeededRNG·FSM·Canvas 렌더 검증. 혼자 플레이되는 죽림고수.
3. ✅ **죽림고수에 멀티 부착, 전체 플로우를 끝까지 돌린다** — 방·시드 동기화·순위·관전.
   ⚠️ 싱글만 돌 때 두 번째 게임을 붙이지 않는다. 멀티까지 전체 플로우가 한 바퀴 돌아야 코어가 검증된다.
4. ✅ **멈추고 훑고 청소한다** — core에 죽림고수 냄새(예: GameRunner가 "화살"을 안다)가 새어들었는지 점검·제거.
   여기서 나온 것: 범용 `StateMachine` 추출, app을 AppFlow/AppView/GameSession으로 분해,
   서버를 `ArcadeServer` + `validation`으로 분리, Vitest 회귀 스위트.
5. ✅ **두 번째 게임(커브 피버)을 붙이며 IGame이 안 맞는 부분을 넓혔다** — 싱글·장애물·멀티·관전
   재구성까지 완주. 결론:
   - `InputState`(4방향) — **안 넓혔다.** 커브는 좌/우만 쓰는 부분집합이었다.
   - `PeerState`(숫자 둘) — **안 넓혔다.** 커브 꼬리는 받는 쪽이 10Hz 신호를 쌓아 재구성했다.
   - 실제로 넓힌 건 **선택 메서드**뿐이고, 그것도 옵셔널이라 죽림고수는 한 줄도 안 바뀐 채로
     계약을 만족했다. 이후 죽림고수가 자기 방식(아이템·정화 파동)으로 필요한 것만 골라 구현하며
     **두 게임이 서로 다른 부분집합을 쓴다**는 게 실제로 확인됐다(현재 6개:
     `getGauge`·`consumePendingFire`·`applyEffect` / `consumePeerEvent`·`applyPeerEvent` /
     둘 다 쓰는 `consumeSounds`).
   - `core`·서버는 이 과정에서 **게임 슬러그를 하나도 알게 되지 않았다**(디버프·연출·소리 kind
     모두 문자열 중계). 아이템을 4종 넣는 동안 서버는 한 번도 재배포하지 않았다. 소리를 붙일
     때도 마찬가지였다 — `shared`의 옵셔널 메서드 하나 말고는 프로토콜도 서버도 안 건드렸다.
6. ✅ **세 번째 게임(무너지는 바닥)으로 「등록만 하면 된다」를 실제로 시험했다.** 결과는 **대체로 참,
   단 단서 하나**다. 고친 파일은 새 패키지(`games/floor/`) 밖으로 딱 4개였고 **전부 배선이지
   로직이 아니다**:
   - `packages/app/src/GameRegistry.ts` — 항목 한 줄(의도한 지점)
   - `tsconfig.json` · `packages/app/tsconfig.json` · `packages/app/package.json` — 모노레포 배선
     (프로젝트 참조 + 워크스페이스 의존성). npm workspaces는 `packages/games/*`로 이미 글롭이라
     자동으로 잡힌다.

   `shared`·`core`·기존 두 게임·서버·앱 UI는 **한 줄도 안 바뀌었다.** 게임 선택 화면·결과·관전·
   HUD가 전부 자동 반영됐다(HUD 게이지 줄은 `getGauge`가 없어 알아서 숨는다).

   > 새 게임이 계약을 넓히지 않고 얻은 것: **「누른 순간」.** `InputState`는 「누르고 있다」만 주는데
   > 이 게임은 칸 단위 이동이라 눌린 전이가 필요했다. 게임이 이전 tick의 입력을 들고 있다가 비교해
   > 스스로 만들었다 — 즉 **입력 계약을 넓힐 이유가 아니었다.** 커브가 4방향을 안 넓힌 것과 같은 종류의
   > 결론이며, 이걸로 `InputState`는 세 게임에서 세 가지 다른 방식(연속 8방향 / 좌우 회전 / 칸 단위
   > 전이)으로 쓰였다.
7. ✅ 무너지는 바닥의 폰 조작 = **방향키 버튼 넷**(`ButtonInput`). 미는 위젯은
   대각선에서 두 방향을 동시에 내고, 무엇보다 세로를 크게 먹어 폰 세로 화면에서 판을 덮었다.

   > 실기기에서만 드러난 것: **판과 조작이 세로를 나눠 갖지 않았다.** 판은 뷰포트 전체 기준
   > 가운데 정렬인데 조작은 아래 고정이라, 화면이 짧을수록 겹쳤다(390×620에서 29px,
   > 360×540에서 54px). 고친 방법은 조작 유무를 body 클래스로 알리고 CSS가 `#play`의 아래
   > 패딩을 키우는 것뿐이다 — `layoutPlayArea`가 그 패딩을 읽으므로 판 크기는 저절로 따라온다.
   > **여백 상수를 CSS에만 두고 JS가 읽어 가게 해 둔 덕에 고칠 곳이 한 곳이었다.**
8. ✅ **네 번째 게임(숫자 야구)이 계약을 두 군데 넓혔다.** 앞의 셋이 넓힌 곳이 하나도
   없었던 것과 대조적인데, 이유가 분명하다 — **이 게임은 회피 게임이 아니다.**
   - **`IGame.typeKey?(slug)`** — 방향 넷으로는 숫자를 못 친다. `InputState`를 숫자로 넓히지 않은
     이유가 핵심이다: 저건 「누르고 있다」의 **스냅샷**이고 타자는 「누른 순간」의 **연속**이라,
     60Hz로 뽑아 보면 스텝 사이에 눌렸다 떼인 키가 통째로 사라진다. 회피 게임에서 1/60초 누락은
     무해하지만 타자에서는 글자가 빠지는 버그다. 그래서 폴링이 아니라 푸시 통로를 따로 열었다
     (`core/input/KeyEntry.ts`). 무너지는 바닥이 「누른 순간」을 **게임 안에서** 만들어 계약을
     안 넓힌 것과 갈리는 지점 — 저긴 상태에서 사건을 뽑아낼 수 있었고 여긴 아니다.
   - **`ScoreUnit`·`GaugeAlarm`(shared) + `scoreUnit`·`gaugeLabel`·`gaugeAlarm`(config)** —
     기록이 **생존 tick이 아니라 점수**인 첫 게임이라, 같은 자리에 "5.1s" 대신 "240점"이 와야 한다.
     게이지도 뜻이 반대다(커브는 차면 발사, 여긴 비면 끝). 둘 다 표시의 문제이지 판정이 아니라서
     **서버는 한 줄도 안 바뀌었다** — 순위표는 여전히 숫자 하나를 방향대로 세울 뿐이다.

   > ⚠️ 대신 **관전 계약이 실제보다 좁게 적혀 있었음이 드러났다.** 이 게임엔 좌표가 없는데 중계
   > 통로는 숫자 둘(a·b)뿐이라 `getPosition()`이 `{a: 푼 문제 수, b: 점수}`를 싣는다. 한동안
   > "구부렸다"고 적어 뒀지만 굽은 건 코드가 아니라 문장이었다 — 앞의 셋이 전부 판 위를 움직이는
   > 게임이라 "위치"라고 쓴 것뿐이고, 서버도 core도 그 뜻을 모른 채 나르고 있었다. 그래서
   > 계약을 `SpectateSignal`("관전 중계에 실을 숫자 둘, 뜻은 게임이 정한다")로 다시 적었다.
   > 남는 것은 짝 하나다: 무엇을 싣든 `renderSpectator`가 **같은 뜻으로** 읽어야 한다.
   >
   > **뒤이어 코드도 고쳤다(2026-08-06).** 문장만 고치고 필드 이름을 `{x, y}`로 두니, 계약이
   > "뜻은 게임이 정한다"고 말하면서 필드 이름으로는 "좌표다"라고 말하는 상태가 남았다.
   > `{a, b}`로 바꿔 이름에서 뜻을 뺐다. 메서드 이름(`getPosition`)은 남겼다 — 넷 중 셋에게는
   > 여전히 위치를 묻는 일이고, 뜻은 반환 타입이 이미 말한다.
   >
9. ✅ 숫자 야구의 폰 조작 = **화면 숫자판**(네 번째 터치 방식 `keypad`). 앞의 셋과 달리
   **방향을 만들지 않는다** — `InputState`가 아니라 글자 슬러그를 밀어 넣는다. 그래도 `touchControls`
   안에 함께 사는 이유는 하나뿐이다: 조작면이라 **화면 자리를 똑같이 다툰다.**

   > 세로에서는 5열×3줄, 가로에서는 3열×4줄로 흐른다. 귀한 축이 반대라서다 — 세로에서는
   > 세로가, 가로에서는 가로가 모자란다. 폰 자판처럼 3열로 고정하면 세로에서 한 줄을 더 먹는다.
   >
   > **여기서 7번의 문제가 축만 바꿔 그대로 나왔다.** 판은 남는 **가로** 전체 기준 가운데
   > 정렬이라, 좁은 가로 화면에서 조작 위로 올라탔다(667×375에서 9px). 세로에서 `--control-h`로
   > 푼 것과 똑같이 `--control-w`를 두고 `#play`의 왼쪽 패딩을 키워 풀었다.
   > **이건 숫자판만의 문제가 아니었다** — 조이스틱·방향키 버튼도 같은 화면에서 겹치고 있었고,
   > 규칙 하나로 셋이 같이 고쳐졌다. 세로에서 겪은 것을 가로에서 다시 겪고 나서야 이게
   > "조작면은 자기 몫의 **자리**를 갖는다"는 한 가지 규칙임이 분명해졌다.

10. ⬅️ **[현재]** **네 게임이 모인 뒤 한 번 정리했다**(SOLID 점검, 2026-08-05). 게임 자체가
    아니라 **네 번째 게임이 드러낸 것들**을 고쳤다.

    > - `getPosition`의 계약을 `SpectateSignal`로 다시 적고 `syncPeers`를 선택으로 내렸다.
    >   **고친 것은 코드가 아니라 문장이다** — 통로는 처음부터 뜻을 모르는 숫자 둘이었다.
    > - 숫자 야구가 규칙을 두 번 적고 있던 곳(겹침 판단·거부 문구·판정 표기)을 하나로 모았다.
    > - `TouchControls.apply()`가 조작면 수만큼 자라던 것을 `SURFACE_CLASS` 표로 접었다.
    > - `main.ts`(458줄)에서 내용을 가진 넷을 떼어 냈다 — 서버 라우팅·내비·HUD·보고 루프.
    >   남긴 것은 배선과 한 판의 흐름이다.
    >
    > **얻은 것은 줄 수가 아니라 가시성이다.** 서버 라우터가 모듈 전역 변수를 조용히 읽던
    > 자리가 `ServerRouteDeps` 열 개로 적혀 나왔다 — 목록이 길다는 사실 자체가 정보다.

> 3~4단계 이후 배포(9절)까지 마쳐 링크 하나로 플레이 가능한 상태다. 다만 **결정론이 진짜
> 네트워크·기기 차이를 견디는지는 아직 다수 동시 플레이로 검증되지 않았다.** 여전히 남은 숙제다
> — 지금은 혼자 여러 탭으로 여는 수준이라 실제 기기·지연 편차를 못 본다.

---

## 8. 설계 규약 (프로젝트 전반에 적용)

로직 밖에서 반복적으로 지키는 관례들. 뼈대 단계부터 검증됐다.

- **tsconfig 옵션 세트**: `strict`, `noUnusedLocals`, `noUnusedParameters`, `noFallthroughCasesInSwitch`,
  `forceConsistentCasingInFileNames`. (실제로 이 규칙이 뼈대 단계에서 안 쓰인 필드를 잡아냄 → 검증됨)
- **빌드 게이트**: `"build": "tsc -b && vite build"` — 빌드 전 타입 검사를 강제.
- **문자열 유니온 타입으로 오타 차단**: `GameId`(레지스트리 키 유니온), `ScoreDirection` 등을 좁은
  유니온으로 두어 존재하지 않는 값 참조를 컴파일 타임에 막는다.
- **데이터 자동 확장**: `GameRegistry`에 항목을 등록하면 게임 목록이 자동 생성된다. 표시용 데이터가
  아니라 factory까지 담아 "실행"까지 이어진다.
- **게임별 설정을 데이터로 분리**: 게임 튜닝값을 각 게임의 `config.ts`에 데이터로 둔다(로직 불변).
- **문서화 습관**: README에 "새 게임 추가법"을 명시해, 확장 경로를 코드 밖에서도 알 수 있게 한다.

의도적으로 **안 쓴** 것: React(게임 화면은 Canvas 중심 — UI 필요 시 나중에 추가).

---

## 9. 배포 (완료 — 라이브)

"완결"에는 배포까지 포함한다. 링크 하나로 남이 플레이할 수 있어야 한다.

- **프론트(정적)**: Vercel — https://web-arcade-sigma.vercel.app
- **게임 서버**: Cloudflare Workers + Durable Objects — `wss://webarcade.leon770.workers.dev`

### 왜 둘을 따로 올리는가

성격이 정반대다. 프론트는 **한 번 넘겨주고 끝나는 정적 파일**(index.html + CSS + JS)이고,
서버는 **연결을 계속 붙들고 상태를 기억하는 쪽**이다. 정적 파일은 CDN이 공짜로 잘 뿌리고, 상태
있는 연결은 CDN이 못 한다. 섞으면 양쪽 다 나빠진다.

게임은 **서버에서 돌지 않는다.** 각자 브라우저가 같은 시드로 같은 게임을 돌리고, 서버는 "시드는
X다", "이 사람 죽었다", "위치는 여기다" 같은 짧은 사실만 중계한다. 덕분에 서버가 할 일이 JSON
파싱과 브로드캐스트뿐이라, 호출당 CPU 10ms인 무료 티어 안에서 넉넉히 돈다.

### 방 하나 = Durable Object 하나

방 코드로 `idFromName(code)` 하면 전 세계에서 **같은 오브젝트 하나**로 라우팅된다. 여기서 두
가지가 따라온다:

1. **인스턴스 수를 신경 쓸 필요가 없다.** 예전에는 방 상태가 프로세스 메모리(`RoomManager`의
   `Map`)에 있어 "머신은 반드시 1대"라는 제약이 있었다. 2대면 Map도 2개라 같은 방 코드를 쳐도
   다른 머신에 붙은 사람은 "그런 방 없음"을 받았고, 누가 어느 머신에 붙을지는 운이었다.
2. **쓰지 않을 때 잠든다.** WebSocket Hibernation을 쓰면 연결은 유지한 채 오브젝트가 메모리에서
   내려가 유휴 비용이 0이 된다. 대기실에 사람들이 앉아만 있는 시간이 정확히 여기 해당한다.

⚠️ 하이버네이션에서 깨면 **메모리 필드가 비어 있다.** 방 상태는 반드시 `ctx.storage`에 있어야
하고 메모리 필드는 그 캐시로만 취급한다. 복원은 생성자의 `blockConcurrencyWhile`에서 한다.

⚠️ 무료 플랜은 **SQLite 백엔드 DO만** 허용한다(`new_sqlite_classes` 마이그레이션).

⚠️ **배포하면 진행 중인 방이 새 코드로 갈아타며 접속자가 튕긴다.** 플레이 중에 배포하지 않는다.

### ⚠️ `VITE_WS_URL`은 빌드 타임에 박힌다

프론트가 붙을 서버 주소는 Vite가 **번들에 문자열로 굳혀 넣는다.** 런타임에 읽는 값이 아니다.
- 서버 주소가 바뀌면 환경변수만 고쳐선 소용없다 → **`vercel --prod`로 다시 빌드해야** 반영된다.
- 페이지가 https이므로 반드시 `wss://`. `ws://`는 브라우저가 mixed content로 차단한다.

### 배포 시 재빌드 범위

바꾼 패키지에 따라 어느 쪽을 다시 올릴지 갈린다. **`shared`(프로토콜)를 고치면 반드시 둘 다.**
한쪽만 올리면 서버와 클라가 서로 다른 메시지 형식을 쓰게 되어 조용히 깨진다.

| 고친 곳 | 재배포 |
|---|---|
| `app`, `core`, `games` | `vercel --prod` |
| `edge` | `npm run deploy:server` |
| `shared` | **둘 다** |

### ⚠️ 방 만들기는 HTTP다 (CORS 필요)

WebSocket은 **붙는 순간 어느 방인지 정해진다**(URL의 방 코드로 오브젝트가 결정되고, 붙은 뒤에는
옮길 수 없다). 그래서 "연결한 뒤 방을 만든다"가 불가능하고, 방 만들기만 `POST /rooms`로 코드를
먼저 받는 2단계다.

이 때문에 프론트(Vercel)와 서버(Workers) 사이에 **교차 출처 요청**이 생긴다. Worker가
`access-control-allow-origin`을 보내지 않으면 요청은 서버에 도달해 방이 실제로 만들어지는데도
브라우저가 응답 읽기를 막아 "방을 만들 수 없습니다"만 뜬다 — 찾기 고약한 종류의 버그다.

## 10. 하지 않기로 한 것 (그리고 왜)

코드에는 **없는 것**이라 주석으로 남길 자리가 없다. 하지만 "왜 이건 안 만들었지?"는
반드시 다시 떠오르므로 여기 적는다. 각 항목에 **재검토 조건**을 함께 둔다 —
"영원히 안 함"과 "아직 안 함"은 다르다.

### 서버 권위 판정 / 리플레이 검증 — 하지 않는다

기록은 클라이언트가 자기 `score`를 **자기신고**한다. 서버는 상한만 검증한다
(`RoomObject.ts`의 `SURVIVAL_TOLERANCE_TICKS` — 방 경과 시간보다 오래 버텼다는 주장은 거부).
**일찍 죽고 늦게 신고하는 것은 막지 못한다.**

그 상한 검사는 **기록이 시간일 때만 뜻이 있다** — 흐른 tick과 신고값을 직접 견주기 때문이다.
한동안 서버는 단위를 모르는 채 모든 게임에 그 산수를 들이댔고, 그래서 점수가 기록인 게임에서는
**막지도 못하면서 게임 설계를 묶었다**(baseball/config.ts가 "점수를 열 배로 키우면 서버 검사에
걸린다"고 적어 두고 있었다). 모르는 채 가정하면 모르는 쪽이 아니라 **틀린 쪽으로** 넘어진다.

이제 서버는 게임에 대해 **한 가지 사실만** 안다: "이 게임의 기록은 흐른 시간인가"(`timedGames.ts`).
규칙도 점수 계산도 여전히 모른다. 두 자리에서 쓴다 — 방은 자기 `gameId`로, 혼자 기록은
**티켓 발급 때 정해 서명 안에 봉인한 값**으로 판단한다(제출할 때 받으면 "점수형입니다"라고
우기는 것만으로 검사를 끌 수 있다).

⚠️ 그 표는 **방어를 켜는 옵션**이지 게임 등록부가 아니다. 표에 없는 게임은 검사를 건너뛸 뿐
아무것도 막히지 않으므로, 게임을 추가할 때 서버를 배포하지 않아도 된다는 성질은 그대로다.

역설적이게도 이 프레임워크는 결정론이라 **원리적으로 완벽한 검증이 가능하다** — 시드와 입력
시퀀스를 받아 서버가 재생하면 진위를 가릴 수 있다. 그런데 그러려면 **서버가 게임 로직을 돌려야
하고**, 그 순간 "서버는 게임을 모른다"(6절)와 의존성 방향(3절)이 동시에 깨진다. 치트 방지 하나로
아키텍처의 중심 원칙 두 개를 파는 거래는 하지 않는다.

→ **재검토 조건**: 실제로 조작된 기록이 관측될 때.

**전역 랭킹은 멀티가 아니라 혼자 기록으로 간다**(2026-08-03 변경). 앞선 판단은 "증인이 있는 쪽이
안전하다"였지만, 멀티 기록은 **인원수가 곧 난이도**라(선이 늘고, 개인 화살이 붙고, 발사로 방해받는다)
판마다 조건이 달라 애초에 줄을 세울 수가 없다. 조건이 고정된 건 혼자 플레이뿐이다.

대신 혼자 기록에는 증인이 없으므로 **티켓**으로 묶는다 — 서버가 시드와 발급 시각에 서명해 주고,
기록을 낼 때 그 티켓을 함께 낸다(`BoardObject`·`soloRules.ts`). 이걸로 막히는 것과 안 막히는 것은
분명하다: 서명이 있어 **내용 위조**가 막히고, 일련번호가 일회용이라 **반복 제출**이 막히고, 발급
시각이 있어 **시간을 날조한 기록**이 막힌다. 반면 **일찍 죽고 그만큼 기다렸다 신고하는 것**은 여전히
통한다 — 그건 위의 리플레이 검증이 있어야만 막히고, 그 거래는 하지 않는다.

### 관리자 화면 — 만들지 않는다 (삭제 명령으로 대신한다)

순위표를 관리할 웹 화면. 운영자가 **한 명**이고 그 한 명은 이 저장소가 있는 컴퓨터 앞에
있으므로, 로그인·세션·화면을 만드는 값이 명령 한 줄보다 크지 않다. 대신 열쇠 하나로 여는
삭제 엔드포인트(`DELETE /solo/entry`)와 CLI(`npm run board`)를 둔다. 열쇠는
`wrangler secret put ADMIN_KEY`로 심고, **없으면 그 경로가 통째로 404**다 — 설정을
빠뜨렸을 때 문이 열린 채 남는 대신 잠기는 쪽으로 넘어진다.

이건 신고 대응만을 위한 게 아니다. 삭제 수단이 없는 동안은 **운영 순위표에 검증용 기록을
쓸 수 없었다**(지울 방법이 없어 1위에 영구히 남는다). 지우기가 생기면서 그 제약도 풀린다.

**욕설 닉네임에는 지우기보다 이름 바꾸기(`PATCH /solo/entry`)가 먼저다.** 기록을 세운 것
자체는 잘못이 아닐 수 있고, 지우면 순위표에 구멍이 생기지만 이름만 바꾸면 등수가 남는다.
무엇보다 **되돌릴 수 있다** — 지운 줄은 복구할 방법이 없다(이력을 남기지 않으므로).
DO 자체의 30일 시점 복구가 있긴 하나 오브젝트 **전체**가 되돌아가 다른 기록까지 사라지므로
"한 줄 되살리기"용이 아니다. 이미 있는 이름으로는 바꾸지 않는다 — 닉네임당 한 줄 규칙대로
합치면 운영자가 시킨 적 없는 삭제가 조용히 일어난다.

→ 화면을 만들 조건: 폰에서 급히 지워야 할 일이 실제로 생길 때. 그때도 같은 엔드포인트를 쓴다.

**닉네임 단어 필터는 넣지 않았다.** 띄어쓰기·초성·자모 변형으로 쉽게 빠져나가는 데 비해
멀쩡한 이름을 막는 오탐은 확실히 생긴다. 이 규모에서는 거르는 것보다 지우는 게 정확하다.

### 디싱크 감지 (월드 해시 대조) — 보류

각 클라이언트가 월드 상태를 해시해 서버가 비교하면 결정론이 깨진 순간을 잡을 수 있다.
만들다가 중단했다 — **증상이 없는 상태에서 만드는 진단 도구**였기 때문이다. 결정론 자체는
헤드리스 테스트로 이미 검증된다(공통 화살 452발 0.000000px 오차).

→ **재검토 조건**: "화살이 남과 어긋난 것 같다"는 실제 제보. 그때 만들면 이미 재현 사례가
있으므로 도구가 훨씬 정확해진다.

### 관전 대상 고르기 — 만들지 않는다

죽은 뒤 누구를 볼지 목록에서 고르는 UI. 의도적으로 뺐다. 대신 자동 전환 + ← → 키로 넘기기만
둔다. 사망 직후는 감정적으로 격한 순간이라 **선택지를 들이미는 것이 연출을 끊는다.**

→ 재검토 조건 없음. 설계 의도다.

### 게임 중 자동 재접속 — 만들지 않는다

끊긴 사람이 판에 복귀하는 기능. 결정론이라 기술적으로는 가능하다(시드와 시작 시각만 받으면
따라잡을 수 있다). 하지만 **이탈이 이미 무해하게 처리된다** — 남은 사람은 계속 플레이하고,
기록은 보존되며, 방장은 자동 승계되고, 빈 방은 60초 유예 후 정리된다. 복귀해도 이미 죽어 있다.

→ 재검토 조건: 게임 길이가 크게 늘어 중도 이탈의 손실이 커질 때.

### 서버 다중화 — 해결됨 (기록으로 남긴다)

한동안 **구조적 제약**이었다. 방 상태가 `RoomManager`의 `Map`, 즉 프로세스 메모리에만 있어서
2대 이상이면 같은 방 코드를 쳐도 다른 머신에 붙은 사람은 방을 못 찾았다. 누가 어느 머신에
붙을지는 운이라 "될 때도 있고 안 될 때도 있는" 최악의 버그였다.

Cloudflare Durable Objects로 옮기며 사라졌다 — **방 하나 = 오브젝트 하나**이고, 방 코드로
`idFromName(code)` 하면 어느 지역에서 접속하든 같은 오브젝트로 라우팅되기 때문이다.
호스팅 크레딧이 소진된 것이 계기였다.

아래는 그 과정에서 **틀렸던 예상과 배운 것**이다. 다음에 비슷한 이전을 할 때 같은 함정에
빠지지 않으려고 남긴다.

#### 대가: 접속 시점에 방 코드를 알아야 한다

공짜가 아니었다. **WebSocket이 어느 오브젝트로 갈지는 접속 URL로 정해지고, 한번 붙은 소켓은
다른 오브젝트로 옮길 수 없다.** 그런데 기존 흐름은 "일단 접속 → `create_room` 메시지로 방
생성"이라, 방이 생기기 전에는 갈 곳이 정해지지 않아 이 순서가 성립하지 않았다.

그래서 방 만들기가 **`POST /rooms`로 코드를 먼저 받고 그 코드로 접속**하는 2단계가 됐다.
`NetClient`도 함께 바뀌었다 — 이식 초기에 "클라는 한 줄도 안 바뀐다"고 봤던 것은 **틀렸다.**
전송 계층이 투명하다는 가정이 깨지는 지점이다.

Worker가 소켓을 받아 DO로 중계하면 클라를 안 바꿀 수는 있었지만, 그러면 Worker가 연결 내내
살아 있어야 해서 하이버네이션의 이점(유휴 시 무과금)이 사라진다. 클라를 바꾸는 쪽이 쌌다.

#### 중앙 Map이 없어서 다시 설계한 것

`RoomManager`가 하던 두 가지는 플랫폼이 대신해 주지 않는다.

- **코드 충돌 검사** — `while (rooms.has(code))`로 확인할 Map이 없다. 각 오브젝트가 스스로
  선점을 판단하고(이미 쓰는 코드면 409), Worker가 다른 코드로 재시도한다.
- **"없는 방"** — `idFromName(code)`는 코드가 뭐든 **항상** 스텁을 돌려준다. `getRoom()`이
  `undefined`를 주던 것에 해당하는 게 없어서, 스토리지에 방 레코드가 있는지로 판단한다.

빈 방 회수도 바뀌었다. 예전에는 `setInterval`로 10초마다 **모든 방**을 훑었는데, 훑는 주기가
유예보다 길면 "유예는 끝났는데 방은 살아있는" 구간이 생겼다. 지금은 방마다 자기 `alarm()`을
걸어 제 시각에 울린다 — 훑을 목록이 없다는 제약이 **오히려 더 정확한 구조**를 만들었다.

#### 살아있는 타이머를 쓸 수 없다

`setInterval`은 오브젝트를 메모리에 붙들어 하이버네이션을 막는다(= 유휴에도 과금). 그래서
관전 위치 중계는 **들어오는 `player_state`에 얹어** 100ms 스로틀로 내보낸다. 클라가 이미
10Hz로 보내므로 주기가 같고, 아무도 안 보내면 오브젝트가 잠들 수 있다.

⚠️ 그리고 `player_state`에서는 **스토리지에 쓰지 않는다.** 10Hz × 인원이면 무료 티어의 하루
쓰기 한도(10만)를 한 시간에 태운다. 위치는 관전용 근사치라 유실이 무해하고, 상태 전이는
`startTime`에서 재계산된다. 사망만 저장한다(라운드당 한 번뿐이라 싸다).

⚠️ 하이버네이션되면 오브젝트의 **메모리 필드가 날아간다.** 방 상태는 반드시 `ctx.storage`에
있어야 하고 메모리 필드는 그 캐시로만 취급한다. 복원은 생성자의 `blockConcurrencyWhile`에서
하며, 이게 "깨어난 직후 빈 상태로 요청을 받는" 사고를 막는 유일한 장치다. 소켓별 신원도
같은 이유로 `serializeAttachment`에 실어 소켓에 붙인다(Map에 들고 있으면 깨어난 뒤 주인을
알 수 없다).

### 조기 추상화 — 금지 (7절 재확인)

`IGame`은 죽림고수 하나만 보고 만든 **잠정 계약**이다. 두 번째 게임이 실제로 부딪히기 전에는
넓히지 않는다. 이미 굽어 있을 것으로 의심되는 지점은 셋이다: `getPosition()`이 플레이어를
"점 하나"로 가정하는 것, `InputState`가 4방향 고정인 것, `PeerState {a, b}`로는 격자·보드류의
관전을 그릴 수 없는 것. **의심은 기록하되 고치지 않는다** — 두 번째 게임이 어느 쪽을 실제로
요구하는지 보고 정한다.

> **결말(네 게임 뒤).** 셋 중 하나만 실제로 걸렸다. `InputState` 4방향은 숫자 야구가
> 넓히는 대신 **다른 통로**(`typeKey`)로 비켜 갔고, `PeerState {a, b}`는 격자 게임(무너지는
> 바닥)이 붙었는데도 충분했다. 걸린 것은 `getPosition()`이었지만 **고친 것은 계약의 폭이 아니라
> 그것을 적은 말이다** — "점 하나의 위치"라는 서술이 실제보다 좁았을 뿐, 통로는 처음부터 뜻을
> 모르는 숫자 둘이었다(`SpectateSignal`). 조기에 넓혔다면 셋 다 잘못 넓혔을 것이다.
>
> 다만 말만 고치는 것으로는 끝나지 않았다. 필드 이름이 `{x, y}`로 남아 있는 한 타입이 계속
> "좌표다"라고 말하므로, 나중에 `{a, b}`로 바꿨다(2026-08-06). **넓힌 게 아니라 이름에서
> 뜻을 뺀 것**이라 계약의 폭은 그대로다.
